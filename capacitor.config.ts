import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.valentimm.eventphotofeed',
  appName: 'Feed do Evento',
  webDir: 'dist',
}

export default config
